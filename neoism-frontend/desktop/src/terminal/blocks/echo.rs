//! Desktop re-export of the shared `terminal_blocks::echo` module.
//! Echo-row detection is pure logic used by `chrome` to suppress the
//! shell's command echo when block chrome paints the same label.

#[allow(unused_imports)]
pub use neoism_ui::terminal_blocks::echo::*;
